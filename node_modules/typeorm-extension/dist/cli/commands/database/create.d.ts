import { Arguments, Argv, CommandModule } from "yargs";
export interface DatabaseCreateArguments extends Arguments {
    root: string;
    connection: 'default' | string;
    config: 'ormconfig' | string;
    synchronize: string;
    initialDatabase?: unknown;
}
export declare class DatabaseCreateCommand implements CommandModule {
    command: string;
    describe: string;
    builder(args: Argv): Argv<{
        root: string;
    } & {
        connection: string;
    } & {
        config: string;
    } & {
        synchronize: string;
    } & {
        initialDatabase: unknown;
    }>;
    handler(raw: Arguments, exitProcess?: boolean): Promise<void>;
}
